# Copia este archivo como .env.local y reinicia Vite (npm run dev)
# API local (Django): python manage.py runserver
VITE_API_BASE_URL=http://127.0.0.1:8000

# Producción (Render) — requiere desplegar el backend con GET /api/etl/lugares/
# VITE_API_BASE_URL=https://limpiadatasets-etl.onrender.com
